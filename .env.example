# Supabase Configuration
# Create a project at https://supabase.com and get these values from Settings > API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Basic Authentication (for demo/staging environments)
# VITE_AUTH_USERNAME=admin
# VITE_AUTH_PASSWORD=your_secure_password

# Optional: GitHub Repository (for Docker builds that clone from GitHub)
# GITHUB_REPO=your-username/your-repo-name

# Optional: Webhook token (if using n8n or similar)
# VITE_N8N_WEBHOOK_TOKEN=your-webhook-token
